//    // Метод для заповнення матриці рандомними значеннями
//    public static double[][] randomFillMatrix(int matrixSize) {
//        double[][] matrix = new double[matrixSize][matrixSize];
//
//        for (int i = 0; i < matrixSize; i++) {
//            for (int j = 0; j < matrixSize; j++) {
//                matrix[i][j] = Math.random(); // Заповнюємо рандомним значенням
//            }
//        }
//
//        return matrix;
//    }
//
//    // Метод для заповнення матриці конкретним(єдиним) значенням
//    public static double[][] fillMatrixWithValue(int matrixSize) {
//        double[][] matrix = new double[matrixSize][matrixSize];
//        Scanner scanner = new Scanner(System.in);
//        System.out.print("Enter value for matrix: ");
//        int value = scanner.nextInt();
//
//        for (int i = 0; i < matrixSize; i++) {
//            for (int j = 0; j < matrixSize; j++) {
//                matrix[i][j] = value; //Заповнюємо введеним значенням
//            }
//        }
//
//        return matrix;
//    }

public static double[] randomFillVector(int vectorSize) {
        double[] vector = new double[vectorSize];

        Arrays.fill(vector, Math.random());

        return vector;
    }

    public static double[] fillVectorWithValue(int vectorSize) {
        double[] vector = new double[vectorSize];
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter value for vector: ");
        int value = scanner.nextInt();

        Arrays.fill(vector, value);

        return vector;
    }